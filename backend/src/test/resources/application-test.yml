spring:
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false

  flyway:
    enabled: true
    locations: classpath:db/migration

  security:
    oauth2:
      client:
        registration:
          google:
            client-id: test
            client-secret: test

app:
  frontend-url: http://localhost:5173
  allowed-domain: test.com
